spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: postgres
    password:
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql
server:
  port: 8081